# 项目规则

1. **验证与运行限制**
   - 除非用户明确要求，否则不得自动运行 Debug 或 Release 编译。
   - 除非用户明确要求，否则不得编写、生成或补充测试用例。
   - 默认仅进行与改动范围相符的语法检查或静态检查。
   - 除非用户明确要求，否则不得进行真实窗口视觉验收，包括为截图、观察界面或判断问题而启动项目、置前窗口或抓取运行界面。
   - 即使正在排查 UI 或平台特定问题，也不得自行启动应用进行 GUI 调试；只有用户在当前任务中明确要求启动时才允许执行。

2. **编译脚本与环境说明**
   - 将 Debug 和 Release 的编译命令分别整理为可直接执行的脚本，并统一放入 `scripts/` 目录。
   - 如果编译前需要安装依赖、设置环境变量、初始化工具链或执行其他环境配置，应将完整的环境配置命令和对应的编译命令写入 GitHub Wiki 的 `Building` 页面，并在 README 保留入口。
   - Wiki `Building` / `Build-and-Release` 中记录的命令应与 `scripts/` 中的实际脚本保持一致；脚本或环境要求变化时必须同步更新。

3. **技术判断原则**
   - 不要因用户的引导而忽略技术原理、事实依据或逻辑是非。
   - 回答问题或修改代码前，应独立核验用户前提与技术可行性。
   - 若用户的判断存在错误、矛盾或风险，应明确指出并说明技术原因，不得迎合、臆测或胡乱修改代码。

4. **UI 布局层级文档**
   - 将 UI 布局层级信息维护在 GitHub Wiki 的 `UI-Layout` 页面：https://github.com/zhaiyanqi/vclogg2/wiki/UI-Layout；`doc/` 仅保留官网页面和资源。
   - 使用树状层级表达布局关系，并以“控件名称 + 唯一编号 + 备注”的形式记录节点，例如：`文件菜单 [A02] — 主菜单入口`。
   - 编号应稳定且唯一；同一控件在后续更新中不得无故更换编号。
   - 新增、删除、移动、重命名控件，或修改布局层级后，必须及时刷新对应文档，确保文档与当前实现一致。
   - 参考格式：

     ```text
     AppShell [A001] — 应用外壳
     └─ AppHeader [A002] — 顶部区域
        └─ MenuRow [A003] — 菜单栏
           └─ 文件菜单 [A004] — 主菜单入口
              ├─ 打开文件 [A005] — 打开本地文件
              └─ 退出 [A006] — 退出应用
     ```

5. **动画设置**
   - 应用内所有动画始终按照产品定义的动画参数执行。
   - 不得读取、绑定或跟随操作系统的“减少动画”、动画开关或同类辅助功能设置来禁用、缩短或替换动画。

6. **产品行为原则**
   - 遇到逻辑或交互不清晰的情况时，以当前产品需求、界面文档和现有架构为依据补齐设计。
   - 新实现应符合当前项目的技术栈、架构和代码风格，并同步更新相关说明文档。

7. **本地协作资料**
   - 搜索收录操作记录、大模型对话、提示词、技能和协作过程记录仅保留在本地，不提交到 GitHub 或 Wiki，也不放入网站发布目录 `doc/`。
   - 新增此类记录统一放在已忽略的 `.local/notes/`；本规则和 `.agents/` 等工具配置保留在原路径并由 Git 忽略。
   - 提交前检查暂存内容，不能强制添加上述本地资料；公开文档不链接本地文件，CI 不依赖本地技能文件。

8. **外部依赖源码边界**
   - 样式、交互和产品能力优先通过外部库公开 API、主题配置、组合、应用层包装或应用自有渲染实现，业务适配放在本项目上层。
   - 不得自行修改 `gpui-component`、`gpui-base`、GPUI 或其他外部库源码。此限制同样适用于外部仓库、Cargo 的 git/registry 缓存、`vendor/` 副本、源码移植及 fork；不能用复制源码或 Cargo `[patch]` 绕过。
   - 不得因缺少 API、修复样式、通过编译或完成普通功能请求而自行增加或扩展外部源码补丁。公开接口不足时，先说明已核验的限制及上层替代方案；只有用户针对具体外部库源码修改另行明确授权，才可变更该边界。普通的修复、实现、验证或提交授权不等于修改外部库源码的授权。
   - 审查存量外部库修改时，记录来源版本、引入及后续提交、实际文件差异、当前是否生效、调用方和迁移影响。不能把源码移植描述成未改外部库，也不能把当前缓存干净描述成历史从未修改。
   - 存量补丁的移除或上移必须按功能逐项验证，保留已有用户改动；不能在审查过程中直接批量删除依赖修复，造成既有功能或构建回退。
