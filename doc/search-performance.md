# 搜索性能优化与验证

## 目录搜索调度与缓存写入

文件准备线程通过原子游标动态领取路径，保持最多四个工作线程和最终输入顺序；取消后不再领取待处理文件。目录扫描完成后的索引缓存由独立单线程写入，队列最多保留两个待写索引，队列满或创建线程失败时跳过可重建的缓存，搜索结果不等待磁盘同步。正在写入的缓存允许完成，取消的待写任务跳过。

验证：阻塞首个文件时其他线程仍能领取其后续路径；阻塞缓存写入时队列保持两项上限且提交不等待；保留顺序与取消回归。

## 密集结果投影

命中超过完整源文件一半行时共享原始行索引和压缩行号，通过源行选择读取；稀疏结果仍仅保留命中的起止偏移，并直接构造压缩集合，去除临时原生行号数组。投影与嵌套投影保留源行坐标、BOM 和末尾空行语义。

验证：密集投影的索引及压缩结果与原值共享同一 Arc；搜索、缺失行、末尾空行及嵌套稀疏投影回归。

## 联合索引搜索的超长行

每个块先检查是否拥有行起点，无行起点的中间块不补读行尾；恰好以 CR 或 LF 结束的块也不额外读取下一行。跨块行只由拥有其起点的块补读与匹配，索引和完整性摘要仍覆盖全部源块。

验证：跨六块长行的四个中间块缓冲均保持单块大小；联合搜索与普通搜索在 CR、LF、跨块 CRLF、末尾空行上的结果一致。

## 重复查询缓存

当前文件、全局与目录搜索共用进程内 LRU，最多 16 项，压缩结果及查询文本合计最多 8 MiB，不保留文档索引或源句柄。键包含路径、完整内容摘要、编码、文件大小及全部查询选项；仅缓存完整且已验证的快照。目录已有候选时先加载索引再验证缓存，未命中时仍走联合索引搜索。

缓存复用前后检查路径变化；没有强变更令牌的平台重新读取并校验源块，忽略可见行热缓存。取消、替换、截断和改写不会把过期缓存当作完成结果。Unix 上节省的是匹配与结果构建，仍有校验 I/O。

验证：同内容重开复用结果 Arc；可见块已缓存后改写仍拒绝旧结果；查询选项、数量上限、取消、LRU 淘汰与容量上限回归。

## 搜索进度与临时预览

当前文件和已打开文件搜索按扫描行数发布进度；目录按完成文件数推进，枚举期间保持不定进度。核心每次搜索最多捕获三条、每条最多 160 字符的代表性命中，目录在文件完成后提供同样有界的预览。预览按任务到达顺序采样，不承诺是最早三条源行。

SearchController 单独拥有临时状态和 100 ms 采样任务，仅在快照变化时刷新 UI。搜索工具栏下显示进度与临时预览，原有正式结果继续保留；完成、取消或切换任务清除临时反馈，旧 revision 的更新被拒绝。缓存命中直接发布完成计数。

验证：预览在扫描完成前可读取且有字符/条数上限；重试重置采样；目录进度有界；控制器拒绝旧任务、去除重复通知并在取消和完成时清理。未启动真实窗口验收。

## 非 UTF-8 转码

UTF-16 在完整且连续的行边界组成最多 256 行、通常最多 64 KiB 原文的批次，一次转码后按 UTF-8 行范围借用文本；投影间隙终止批次。单条超长行允许超出批次字节限制，其大缓冲在下一批释放。Legacy 编码保持原有的逐行解码器重置语义，同时复用批次输出缓冲，避免状态型编码跨行行为变化。UTF-8 仍直接借用源字节。

验证：UTF-16LE/BE 的 CR、LF、CRLF、代理对、不完整末尾编码与稀疏投影逐行对照旧解码结果；GBK 和 ISO-2022-JP 逐行行为保持一致；转码结果以借用切片返回，批次行数受限。

## 前 N 条结果提前停止

小上限先扫描最多 4096 行、通常不超过 256 KiB 的前缀；若已经找到 N+1 条，直接结束，避免启动其他文件块扫描。前缀未满足时保留并行扫描，仅处理其后的行。并行任务只在按源顺序连续完成的前缀证明已有 N+1 条后，停止更晚的任务；晚完成顺序不会抢占早期行的名额。多取一条仅用于准确判断截断。

联合索引搜索每块最多保留 N+1 个命中起点，并采用相同的有序停止信号；全部索引和摘要仍完整生成，省去的是后续匹配及命中存储。无限制搜索维持原有并行路径。

验证：四线程、多块密集文件取 0/1/17 条只读取首块；4096 条、稀疏命中、并行正则及零上限回归；乱序完成与恰好 N 条不提前判定截断；联合索引单块只存 N+1 条且后续块跳过匹配。

## 本轮验证记录（2026-09-05）

七项分别运行针对性回归后提交；各项均运行工作区测试、格式/架构/差异检查及编译检查。最终另对空查询无 I/O 和空文件增长的缓存失效补充回归。最终工作区测试 267 项通过、11 项标记忽略；其中两项 Release 搜索基准另行手动执行通过。

严格 core Clippy 通过。工作区严格 Clippy 被既有 `global_search_table.rs` 的 `install_staged_visible_lines` 未使用警告阻挡；仅放宽 `dead_code` 后其余 Clippy 检查通过。没有启动应用或做真实窗口验收。

在同一台机器顺序执行相同 Release 基准，每项一百万行、五次平均；基线为前三项优化后的 `128289e`，以下是本轮七项全部改动后的结果，未启用重复查询缓存或结果数量上限：

| 基准 | 基线 | 本轮 |
| --- | ---: | ---: |
| 普通文本搜索 | 43.36 ms | 42.88 ms |
| 正则，1 线程 | 130.63 ms | 134.02 ms |
| 正则，8 线程 | 45.38 ms | 44.19 ms |

这些差异较小，不作为通用加速比例；具体场景的收益由前文的读取次数、共享存储、队列容量和转码批次回归确认。
