# VCLogg2 UI 布局层级

本文档是当前 GPUI 界面层级与稳定控件编号的可读合同。新增、删除、移动或重命名控件时，必须在同一提交内同步更新。

```text
AppShell [A001] — 每个窗口唯一的 Root 工作区
├─ FileDropTarget [A128] — 覆盖整个工作区；接收一个或多个外部文件路径，忽略文件夹并复用普通后台打开链路
│  └─ FileDropOverlay [A130，外部文件悬停窗口期间] — 主题 Popover 表面与强调色虚线边界；说明松开结果和文件夹处理规则，不改变底层布局
├─ TabTransferDropHint [A146，跨窗口标签悬停期间] — 右上角紧凑 Popover 提示移动/复制语义；标签栏保持可见以选择插入位置
├─ TitleBar [A002] — 原生桌面窗口标题栏；覆盖组件默认底边框，与下方工具栏连续衔接；macOS 窗口模式为三色窗口按钮保留约 80px 外层起始间距，全屏时该外层占位归零，组件内部仍保留约 12px 紧凑边距
│  ├─ MenuBar [A321] — 与窗口标题栏共用同一水平带；菜单按钮使用独立的 12px 安全行高文字层，标准 PopupMenu 负责键盘导航、焦点恢复与关闭，并以已挂载的工作区根焦点在首帧完成快捷键与宽度布局
│  │  ├─ 文件菜单 [A322]
│  │  │  ├─ 打开日志 [A323] — 复用 Ctrl+O 命令
│  │  │  ├─ 新窗口 [A324] — 复用 Ctrl+Shift+N 命令
│  │  │  ├─ 重新加载 [A325] — 复用 F5 命令；无活动文件时禁用
│  │  │  ├─ 在文件资源管理器中显示 [A404] — 复用标签菜单的“打开所在目录”链路，作用于活动文件
│  │  │  ├─ 管理历史 [A327] — 按需读取完整文件会话摘要并打开历史对话框
│  │  │  ├─ 关闭标签 [A326] — 复用 Ctrl+W 命令；文件标签与“新标签页”使用同一活动标签关闭链路
│  │  │  ├─ 关闭其他标签页 [A405] — 复用 A122 的批量关闭链路；少于两个标签时禁用
│  │  │  ├─ 关闭全部标签页 [A406] — 复用 A125 的批量关闭链路；完成后自动建立一个“新标签页”
│  │  │  └─ 退出 VCLogg2 [A407] — 请求应用退出，仍走各窗口的关闭确认与会话保存
│  │  ├─ 编辑菜单 [A328]
│  │  │  ├─ 复制当前行 [A329]
│  │  │  ├─ 复制当前行（含行号） [A330]
│  │  │  ├─ 复制文件路径 [A332]
│  │  │  ├─ 全选行 [A331]
│  │  │  ├─ 查找 [A408] — 聚焦搜索框并全选查询文本；Ctrl+F
│  │  │  ├─ 转到行 [A333] — 复用 Ctrl+G 命令
│  │  │  └─ 清除搜索结果 [A409] — 按当前搜索范围清空当前文件或全局结果
│  │  ├─ 视图菜单 [A334]
│  │  │  ├─ 自动换行 [A335] — 显示当前选中状态
│  │  │  ├─ 显示行号 [A336] — 显示当前选中状态
│  │  │  ├─ 日志分隔线 [A337] — 显示当前选中状态
│  │  │  ├─ 显示完整路径 [A410] — 与 A247 同一应用级设置；切换后立即落盘
│  │  │  ├─ 跟随末尾 [A338] — 显示当前选中状态
│  │  │  ├─ 文件开头 [A339]
│  │  │  ├─ 文件末尾 [A340]
│  │  │  └─ 进入全屏 / 退出全屏 [A341] — 打开“视图”菜单时按当前窗口状态动态显示“进入全屏 / Enter full screen”或“退出全屏 / Exit full screen”；macOS 使用标准 `⌃⌘F`，全平台保留 `F11` 兼容入口
│  │  ├─ 工具菜单 [A411]
│  │  │  ├─ 预定义过滤器 [A412] — 打开预定义过滤器对话框
│  │  │  ├─ 清除搜索历史 [A413] — 一次清空并同步所有窗口；无历史时禁用
│  │  │  └─ 设置 [A343] — 直接打开设置对话框并恢复上次分类；Ctrl+,
│  │  ├─ 高亮菜单 [A344]
│  │  │  ├─ 日志级别着色 [A414] — 与设置页同一应用级开关；显示当前选中状态
│  │  │  ├─ 高亮搜索匹配 [A415] — 与设置页同一应用级开关；显示当前选中状态
│  │  │  ├─ 标记行 [A345]
│  │  │  ├─ 轮换颜色 [A346] — 复用 Ctrl+D 命令
│  │  │  ├─ 颜色标签 [A347] — 打开全局标签定义对话框
│  │  │  ├─ 搜索时区分大小写 [A416] — 与 A036/A253 同一状态；显示当前选中状态；Alt+C
│  │  │  ├─ 使用正则表达式 [A417] — 与 A037/A254 同一状态；显示当前选中状态
│  │  │  └─ 清除搜索高亮 [A418] — 与 A409 同一清除链路
│  │  ├─ 编码菜单 [A348] — 无活动文件或索引任务期间禁用
│  │  │  ├─ 当前编码 [A349] — 只读显示检测名称
│  │  │  ├─ 重新检测并加载 [A350]
│  │  │  └─ 复制编码名称 [A351]
│  │  ├─ 收藏菜单 [A352]
│  │  │  ├─ 收藏当前文件 [A353] — 收藏后切换为“取消收藏文件”并显示选中状态
│  │  │  ├─ 收藏文件 [A419，可重复] — 按收藏顺序列出；点击复用最近文件打开链路；无收藏时显示禁用的“暂无收藏文件”
│  │  │  └─ 清空收藏 [A420] — 复用 A078 的清空链路；无收藏时禁用
│  │  └─ 帮助菜单 [A354]
│  │     ├─ 键盘快捷键 [A421] — 打开设置对话框并直接定位“快捷键”分类
│  │     ├─ 更新 [A355] — 在系统浏览器中打开项目 GitHub Releases 页面
│  │     └─ 关于 ver.<当前版本> [A403] — 紧跟当前构建版本号，并打开设置对话框的“关于”分类
│  └─ 应用标题 [A003] — 居中显示 VCLogg2
├─ FileToolbar [A011] — 菜单栏下方的紧凑文件信息带；顶部不绘制分隔线，与菜单栏连续衔接；只保留高频快捷入口与活动文件必要信息
│  ├─ 打开日志 [A012] — 图标快捷入口；原生文件选择框，可多选；Ctrl+O
│  ├─ 重新加载 [A045] — 图标快捷入口；原子替换文件快照并恢复当前搜索；F5
│  ├─ 收藏文件 [A075] — 图标快捷入口；收藏时显示填充星形
│  ├─ 文件末尾 [A015] — 图标快捷入口；Ctrl+End
│  ├─ 当前文件路径 [A014] — 带文件图标的单行边框区域；按设置显示完整路径或标签名
│  ├─ 文件大小 [A356] — 活动文件真实字节数的紧凑可读值
│  ├─ 编码菜单 [A184] — 显示当前自动识别编码并打开标准 PopupMenu；索引任务期间禁用
│  │  └─ 编码菜单弹层 [A185]
│  │     ├─ 当前编码 [A186] — 只读显示 UTF-8、UTF-8 BOM、UTF-16、传统编码名称或 Binary
│  │     ├─ 重新检测并加载 [A187] — 保留旧快照，后台重新检测与完整索引成功后原子换入
│  │     └─ 复制编码名称 [A188] — 把当前检测名称写入剪贴板
│  └─ 行位置 [A357] — 显示当前源行与活动文件总行数；未选中时从第 1 行开始
├─ DocumentTabBar [A016] — 文件与“新标签页”共用的常驻标签栏；48px 栏高、36px 标签（gpui-component Large 档）与 12px 标题文字；标签不设置额外最小或最大宽度，完全按内容自然布局，超出可用区域时由标签栏横向滚动
│  ├─ 新建标签页 [A436] — 紧跟最后一个标签的 Plus 图标 Button，并随标签列表横向滚动；创建并激活一个不持久化的“新标签页”
│  ├─ 所有标签菜单 [A358] — 使用标准 PopupMenu 列出文件与“新标签页”的完整名称并标记活动标签，替代 TabBar 内置标题菜单
│  └─ DocumentTab [A017，可重复] — 使用文档 ID 保持稳定身份；20px 文件图标、采用安全行高的完整文件标题与关闭按钮位于同一紧凑内容组，加载阶段由当前文件内容摘要反馈
│     ├─ 文件图标 [A367，可重复] — 使用与 vclogg2 相同的 Fluent DocumentText20Regular 资源；跟随标签前景色并以轻微透明度弱化装饰性层级
│     ├─ 标签拖动命中区 [A136，可重复] — 窗口内使用类型安全负载排序；离开窗口后由源窗口捕获指针并解析其他 VCLogg2 窗口；内部命令按钮保持各自交互
│     ├─ 标签前插入目标 [A137，可重复] — 悬停时使用主题强调色左插入线；放下不切换活动文件
│     ├─ 标签右键命中层 [A145，可重复] — 覆盖标签正文区域，以文档 ID 稳定标识并直接打开与 A120 相同的 PopupMenu
│     │  └─ 标签操作菜单 [A120] — 菜单动作始终绑定触发标签，不依赖当前活动标签
│     │     ├─ 关闭此标签 [A121]
│     │     ├─ 关闭其他标签 [A122] — 只有一个标签时禁用
│     │     ├─ 关闭左侧标签 [A123] — 触发标签位于首位时禁用
│     │     ├─ 关闭右侧标签 [A124] — 触发标签位于末位时禁用
│     │     ├─ 关闭所有标签 [A125]
│     │     ├─ 复制完整路径 [A126]
│     │     ├─ 打开所在目录 [A127] — 空命令交给 GPUI 平台系统打开能力；配置 A298 时直接启动结构化命令
│     │     ├─ 复制到新窗口 [A141] — 捕获当前标签会话，在新原生窗口重新索引并恢复显示状态；源标签不变
│     │     ├─ 移动到新窗口 [A142] — 目标安装成功且源会话未变化后才卸载源标签；否则安全退化为复制
│     │     ├─ 移动到另一窗口 [A143] — 发送到最近使用的其他窗口；无目标时禁用，目标忙碌、重复或关闭时保留源标签
│     │     ├─ 复制到另一窗口 [A144] — 发送到最近使用的其他窗口；无目标时禁用且不改变当前标签
│     │     ├─ 重命名标签 [A131] — 打开 A133；省略号表示提交前还需输入
│     │     └─ 恢复标签名称 [A132] — 没有自定义名称时禁用；立即恢复磁盘文件名
│     └─ 关闭此标签 [A018，可重复] — 使用文档 ID 作为按钮身份
│  ├─ NewWorkspaceTab [A435，可重复] — 名称固定为“新标签页”，使用独立稳定 ID；支持激活、关闭、中键关闭、窗口内排序和标签级批量关闭，不提供文件路径、删除、重载、重命名或跨窗口传输命令
│  │  └─ 关闭此标签 [A018，可重复] — 使用新标签 ID 作为按钮身份；关闭最后一个标签时立即补建一个新标签页
│  ├─ 标签末端放置区 [A138] — 把标签移动到当前顺序末尾
│  └─ 标签拖动预览 [A139，拖动时] — 使用工作区标签 ID 保持稳定身份的主题化浮层；新标签页只接受窗口内排序
├─ WorkspaceBody [A004] — 主工作区域；只允许实际数据区滚动
│  ├─ NewTabWorkspace [A019，仅活动标签为 A435 时] — 原空工作区内容成为“新标签页”的正文
│  │  └─ EmptyWorkspaceScroll [A080] — 空工作区唯一纵向滚动所有者；短内容仍在视口居中
│  │     ├─ 空状态标题区 [A021] — “开始查看日志”，说明可打开日志或从最近文件继续；标题与说明均使用常规字重和紧凑字号
│  │     ├─ 打开日志文件 [A023] — 紧凑桌面密度的蓝色主入口，打开原生文件选择框
│  │     ├─ PinnedFiles [A076] — 收藏文件清单；最多直接显示 8 项
│  │     │  ├─ 收藏文件标题 [A077] — “收藏文件”
│  │     │  ├─ 清空收藏 [A078] — 只清除文件级固定状态，不删除会话或文件
│  │     │  ├─ 收藏文件项 [A079，可重复] — 数据库会话 ID 构成稳定身份；单行固定为文件名、父目录、打开时间三列，单独后台打开
│  │     │  └─ 收藏折叠数量 [A081，超过 8 项时] — 说明未直接列出的收藏数量
│  │     ├─ LastWorkspace [A070] — 正常退出时保存的标签顺序清单；启动后不自动索引
│  │     │  ├─ 上一次文件标题 [A071] — “上一次文件”
│  │     │  ├─ 全部打开 [A073] — 按保存顺序后台打开全部文件并恢复上次活动标签
│  │     │  ├─ 上一次文件项 [A072，可重复] — 数据库会话 ID 构成稳定身份；单行固定为文件名、父目录、打开时间三列，单独后台打开
│  │     │  └─ 折叠数量说明 [A074，超过 8 项时] — 说明其余文件可通过“全部打开”进入
│  │     └─ RecentFiles [A066] — SQLite bootstrap 完成后最多显示 8 项；收藏优先、再按最近打开时间倒序
│  │        ├─ 最近文件标题 [A067] — “最近文件”
│  │        ├─ 查看全部 [A437] — 打开文件历史对话框；仅在最近文件非空时显示
│  │        ├─ 最近文件项 [A068，可重复] — 数据库行 ID 构成稳定身份；单行固定为文件名、父目录、本地月日时间三列，打开时恢复文件会话
│  │        └─ 最近文件状态 [A069] — 非阻塞加载或空状态
│  └─ DocumentSplit [A024，仅有活动文档时] — 可纵向调整的正文/结果区域；搜索面板调整事件层是分栏的顶层同级节点，按 GPUI 顶层 hitbox 判定后才进入捕获拖拽；Ctrl+滚轮按 1px 步长调整全局日志字号并防抖持久化
│     ├─ 页内查找栏 [A149，Ctrl+Shift+F 后] — 条件挂载到用户最后一次实际点击的正文 A025、当前结果 A032 或全局结果 A101 的右上角，使用不透明浮层与输入框表面且不占用表格布局；结果点击引起的正文联动定位、选择事件或程序化 FocusHandle 不覆盖这份用户区域记录；锁定打开时的所属区域，关闭前不随主搜索范围切换
│     │  ├─ 页内查找输入框 [A150] — 单个完整查询；50ms 防抖后取消旧扫描并后台定位；同一匹配器把金色关键词高亮同时下发到各标签正文、当前文件结果以及全局/目录结果，定位导航仍锁定打开时的目标区域；正文、当前结果或全局结果的程序化选择与联动滚动均不得转移输入焦点；聚焦时由 Input 独占文字与编辑按键，日志标记、换行等应用快捷键不参与匹配；无匹配、正则错误与边界状态同步进入可访问名称
│     │  │  ├─ 匹配大小写 [A463] — 输入框内右侧的 Lucide 切换按钮；默认关闭，切换后立即按新规则重新定位并把焦点保持在输入框
│     │  │  ├─ 全词匹配 [A464] — 输入框内右侧的 Lucide 切换按钮；要求命中区间两侧均不是 Unicode 字母、数字或下划线，切换后立即重新定位
│     │  │  └─ 正则表达式 [A465] — 输入框内右侧的 Lucide 切换按钮；关闭时所有元字符均为字面量，开启后按正则编译，无效草稿就地呈现危险状态且不弹出通知
│     │  ├─ 页内查找状态 [A154] — 当前查询没有命中或正则无效时浮层边框使用 danger 红色；已有命中后到达开头/末尾仍使用 warning 黄色并更新对应方向按钮提示；连续执行同方向命令不重复扫描，也不从另一端循环
│     │  ├─ 查找上一处 [A151] — Shift+Enter / Shift+F3；只在向上扫描时显示 loading，后台忙碌或空查询时禁用
│     │  ├─ 查找下一处 [A152] — Enter / F3；只在向下扫描时显示 loading，后台忙碌或空查询时禁用
│     │  └─ 关闭页内查找 [A153] — Esc；取消扫描并移除页内金色高亮，焦点返回被锁定的正文或结果区域，保留查询供下次打开
│     ├─ LogDataTable [A025] — 隐藏列标题；默认使用纵横虚拟表格；开启 A178 后切换为 gpui-component 可变行高虚拟列表，首帧按基础行高估算并只校准可见行
│     │  ├─ LogicalPositionBar [A237] — 常驻纵向滚动条；使用不透明表格主题轨道背景。关闭换行时沿用表格逻辑轨道；开启换行时由适配层把固定逻辑槽双向映射到可变像素高度，末屏精确落到实际最大偏移
│     │  ├─ 行标记列 [A051，始终存在] — 位于行号之前的固定窄列；普通行为描边空心圆，搜索命中为 danger 红色实心圆与弱光晕，用户标记为 warning 黄色实心圆与弱光晕，同时命中且标记时在同一圆内显示 135° 红黄分区；隐藏行号不会隐藏本列且复制不包含本列
│     │  ├─ 行号列 [A026] — 固定左列、右对齐
│     │  ├─ 日志列 [A027] — 使用主题等宽字体；Root 文字选择层按源行稳定顺序注册当前虚拟可见文字
│     │  ├─ LogRow [A028，可重复] — 文档 ID + 源行号构成稳定身份
│     │     ├─ ColorLabelDecoration [A165，条件呈现] — 文件级大小写敏感字面规则使用标签数据色；同一区间优先于主搜索并同步到当前/全局结果
│     │     ├─ SearchMatchDecoration [A065，条件呈现] — 已提交查询的实际非空命中使用主题警告色文字背景；页内查找覆盖同一区间时使用更醒目的金色
│     │     └─ TextSelectionDecoration [A176，条件呈现] — 使用主题强调色覆盖实际字形选区；正文、当前结果与全局结果各有独立作用域
│     │  └─ LogContextMenu [A358，右键日志行或正文空白]
│     │     ├─ 复制 [A359] — 有文字选区时复制文字，否则复制当前区域所选日志行
│     │     ├─ 颜色标签 [A360] — 标准子菜单；包含“无”[A361]、带实际色块与右侧选中勾选的可用标签 [A362，可重复] 与“清除所有颜色”[A363]
│     │     └─ 标记/取消标记 [A364] — 根据稳定菜单上下文显示对应动词
│     ├─ LogHorizontalScrollbar [A474，关闭自动换行时] — 位于 LogDataTable 底部的独立同级区域；横跨整个日志区域并让右端贴齐区域边界，不再为纵向滚动条保留底部空白槽；不覆盖日志行，固定标记/行号列仍保持可见；使用不透明表格主题轨道背景且不绘制顶部边框
│     └─ SearchPanel [A258] — 工作区底部的第二个可调高度面板；搜索工具栏和结果内容共享同一面板边界与生命周期
│        ├─ SearchPanelResizeHitArea [A481] — 透明行高调整命中层；从搜索输入框上边缘向上覆盖日志横向滚动条底部 1/3；DocumentSplit 同级事件层明确锚定左上角并覆盖整个分区，只有它在当前坐标仍为顶层命中目标时，才于捕获阶段独占并消费按下、拖动与抬起事件
│        ├─ SearchBar [A034] — 搜索面板首行；当前标签、多标签与目录搜索共用
│        │  └─ SearchControls [A057] — 依次为结果模式、大小写、正则、过滤器、搜索范围、搜索输入、搜索、清除和结果状态
│        │     ├─ 结果显示模式 [A058/A111] — 当前文件使用标签独立模式，全局范围切换只重组已提交匹配与标记
│        │     │  ├─ 标记与匹配 [A059/A112] — 合并、去重并按源行号排序
│        │     │  ├─ 仅匹配 [A060/A113] — 排除仅有标记的行
│        │     │  └─ 仅标记 [A061/A114] — 只显示参与范围内的标记行
│        │     ├─ 区分大小写 [A036] — 可见选中状态；Alt+C
│        │     ├─ 正则表达式 [A037] — 可见选中状态
│        │     ├─ 过滤器 [A195] — 搜索栏中的标准 Popover 触发器，弹层内容为预定义过滤器；打开期间保持选中态，当前文件与全局范围共用，状态库保存期间显示 loading
│        │     │  ├─ 过滤器列表 [A379] — 超过四项时使用固定高度视口的唯一纵向滚动所有者；连续勾选不会关闭弹层
│        │     │  │  └─ 过滤器选项 [A196，可重复] — gpui-component Checkbox；稳定过滤器 ID 构成身份，选中背景与勾选状态由当前查询实时派生
│        │     │  │     ├─ 匹配值预览 [A380，可重复] — 第二行单行截断，完整值通过工具提示可读
│        │     │  │     └─ 正则标记 [A381，条件呈现] — 使用 `.*` 文字与主题强调色标识正则过滤器
│        │     │  └─ 编辑预定义过滤器 [A197] — 弹层固定底部 Button；先关闭弹层再打开 A198
│        │     ├─ 搜索范围 [A097] — 位于过滤器之后的同级紧凑描边分段控件，与过滤器同高；全局/目录范围下右键整组可直接打开对应配置页，右键按下不把焦点转移到分段控件
│        │     │  ├─ 范围选择 [A471] — “图标 + 短文字 + 下箭头”Button；当前文件、全局搜索和目录搜索分别显示“当前”“全局”“目录”，左键打开 A422
│        │     │  │  └─ 搜索范围菜单 [A422] — 标准 PopupMenu；当前范围仅显示一层与原生悬停同尺寸、同圆角的强调背景，不显示对勾或内层背景；未选项悬停完全沿用 PopupMenu 原生样式，键盘确认只切换范围
│        │     │  │     ├─ 当前文件 [A423] — 搜索图标与文字，不显示配置按钮
│        │     │  │     ├─ 全局搜索 [A424] — 文件图标、文字与独立配置按钮
│        │     │  │     │  └─ 选择参与标签 [A425] — 打开 A105；同时把范围切换为全局搜索
│        │     │  │     └─ 目录搜索 [A426] — 文件夹图标、文字与独立配置按钮
│        │     │  │        └─ 设置目录范围 [A427] — 打开 A428；同时把范围切换为目录搜索
│        │     │  └─ 当前范围设置 [A472，全局/目录范围] — 与 A471 同高拼接的设置图标 Button；左键打开当前范围配置，当前文件范围不显示
│        │     ├─ 搜索输入框 [A035] — 占位提示为“搜索”；伸缩并保留最小可见宽度；Enter 执行搜索；Ctrl+F 聚焦并全选
│        │     │  ├─ 搜索图标 [A482] — Input 前缀中的静态放大镜图标，使用弱化前景色且不响应点击
│        │     │  ├─ 搜索补全列表 [A255，条件呈现] — 当前顶层 `|` 片段非空且存在匹配时自动在搜索框下方向下展开；与搜索框等宽并对齐左右边缘，按候选数量自适应为 1–5 行、更多项目虚拟滚动，并进入标准弹层延迟绘制层覆盖搜索结果区域
│        │     │  │  └─ 搜索补全项 [A256，可重复] — 显示金色片段命中及候选来源
│        │     │  └─ 搜索历史菜单 [A147] — 输入框后缀 Button；无历史时禁用，点击后复用 A255 在搜索框下方等宽展开完整历史
│        │     │     └─ 搜索历史项 [A148，可重复] — 选择后回填输入框并全选，不自动提交
│        │     ├─ 执行搜索 [A039] — 默认提交动作，搜索期间显示 loading
│        │     ├─ 清除搜索 [A038] — 图标 Button；清除查询并收起已提交结果
│        │     └─ 已提交结果状态 [A257，存在结果时] — 主题强调色计数胶囊
│        ├─ 搜索面板空状态 [A259，尚无已提交结果时] — 提示输入关键词后开始搜索
│        ├─ SearchResultsPanel [A029，当前文件范围且完成搜索后]
│        │  ├─ ResultDataTable [A032] — 隐藏列标题，仅显示虚拟化日志行；选择后定位正文
│        │  │  ├─ ResultVerticalScrollbar [A375] — 从列表顶部开始并复用表格滚动状态；常驻显示并使用不透明表格主题轨道背景
│        │  │  ├─ ResultRow [A033，可重复] — 文档 ID + 源行号构成稳定身份
│        │  │  └─ ResultContextMenu [A260，右键结果行或结果区空白]
│        │  │     ├─ 公共日志命令 [A365] — 复用 A359–A364；空白且无领域选择时不显示
│        │  │     ├─ 新标签打开结果 [A115] — 后台生成临时结果后复用普通打开链路
│        │  │     └─ 保存结果 [A116] — 系统“另存为”选择目标
│        │  └─ ResultHorizontalScrollbar [A475，关闭自动换行时] — 位于 ResultDataTable 底部的独立同级区域；横跨整个结果区域并让右端贴齐区域边界，不再为纵向滚动条保留底部空白槽；不覆盖结果行，固定列保持可见；使用不透明表格主题轨道背景且不绘制顶部边框
│        └─ GlobalSearchResultsPanel [A098，全局范围且完成过搜索后]
│           ├─ GlobalResultDataTable [A101] — 隐藏列标题，仅按标签顺序显示文件分组与日志行
│           │  ├─ GlobalResultVerticalScrollbar [A377] — 从列表顶部开始并复用表格滚动状态；常驻显示并使用不透明表格主题轨道背景
│           │  ├─ GlobalFileGroup [A102，可重复] — 文档 ID 构成稳定身份；标题占满整行并从左侧图标槽后开始，路径后紧邻显示该文件结果数，无结果时使用文档关闭图标
│           │  ├─ GlobalResultRow [A103，可重复] — 文档 ID + 源行号构成稳定身份
│           │  └─ GlobalResultContextMenu [A261，右键文件分组、日志行或结果区空白]
│           │     ├─ 公共日志命令 [A366] — 日志行复用 A359–A364；文件标题和空白只保留适用的结果命令
│           │     ├─ 新标签打开全局结果 [A117] — 按标签顺序与完整路径分组写入临时文件
│           │     ├─ 按时间合并 [A194] — 将各文件结果稳定多路归并到新临时标签
│           │     └─ 保存全局结果 [A118] — 导出当前模式下全部已提交结果
│           └─ GlobalResultHorizontalScrollbar [A476，关闭自动换行时] — 位于 GlobalResultDataTable 底部的独立同级区域；横跨整个全局结果区域并让右端贴齐区域边界，不再为纵向滚动条保留底部空白槽；不覆盖结果行，固定标记/行号列保持可见；使用不透明表格主题轨道背景且不绘制顶部边框
└─ StatusBar [A007] — 窗口级状态信息
   └─ 行位置/核心版本 [A009] — 当前行、多选数量与标记数；未选择时显示核心版本

RootOverlayLayers [A041] — Workspace 先安装后台事件隔离层，再由每个窗口唯一 Root 统一管理前景弹层
├─ ForegroundPointerBarrier [A483] — 位于标题栏以下、Workspace 内容之上、前景弹层之下的透明命中哨兵；当前坐标被后绘制的 Dialog、Sheet、Popover 或其他 occluder 覆盖时，在前景处理后拦截按下、移动与滚轮事件，鼠标抬起仅放行后台手势清理
├─ DialogLayer [A042] — 对话框与焦点恢复层
│  ├─ 转到行对话框 [A049] — 标准 Dialog + Input，校验 1 到当前总行数
│  ├─ 清除历史确认 [A083] — 标准 AlertDialog；危险提交明确说明保护范围且默认可取消
│  ├─ 设置对话框 [A094] — 与 A085/A167/A198 共用管理类 Dialog 的风格、尺寸和窗口居中规则；有效修改即时预览，保存持久化，取消或关闭恢复原设置
│  │  ├─ 设置分类侧栏 [A309] — 常驻 Sidebar；与详情区域独立布局和滚动
│  │  │  ├─ 设置搜索 [A310] — retained 标准 Input；按中英文标题、说明与关键词筛选分类并定位首个结果
│  │  │  └─ 分类导航 [A311] — 标准 SidebarMenu；选中态持续可见并独立持久化，重新进入时恢复上次分类
│  │  │     ├─ 常规 [A312] — 文件与标签行为
│  │  │     ├─ 网络 [A389] — 云端服务器、用户身份与 Cookie 连接
│  │  │     ├─ 外观 [A313] — 主题、字体、行号与内容呈现
│  │  │     ├─ 搜索 [A314] — 默认匹配方式、结果呈现与搜索历史
│  │  │     ├─ 滚动与交互 [A315] — 滚轮、双击选词与预读取
│  │  │     ├─ 存储 [A317] — 索引缓存
│  │  │     ├─ 快捷键 [A318] — 键盘绑定与冲突检查
│  │  │     ├─ 高级 [A316] — 应用日志等级、诊断缓冲与导出
│  │  │     └─ 关于 [A395] — 版本、构建、技术栈、开源组件与许可信息
│  │  └─ 设置详情 [A319] — 当前分类的唯一纵向滚动区域；内容区随可用窗口高度收缩，底部操作始终可达
│  │     ├─ 语言 [A466] — “常规”页标准纵向 RadioGroup；默认简体中文，修改后即时预览并随设置保存
│  │     │  ├─ 简体中文 [A467] — `zh-CN` 界面资源
│  │     │  └─ English [A468] — `en-US` 界面资源
│  │     ├─ 远程服务 [A390] — “网络”页服务器公开配置与按需 Cookie 连接测试
│  │     │  ├─ 服务器地址 [A391] — retained 标准 URL Input；支持 HTTP/HTTPS，优先恢复 SQLite、既有连接或构建默认地址
│  │     │  ├─ 用户名 [A392] — retained 标准 Username Input；空配置默认读取当前系统用户名，最多 64 个字符
│  │     │  ├─ 保存并测试 [A393] — 标准主按钮；后台授权并把 Cookie/CSRF 留在系统凭据库
│  │     │  └─ 连接状态 [A394] — 就地显示待配置、处理中、成功或可恢复错误
│  │     ├─ 界面主题 [A102] — “外观”页标准纵向 RadioGroup；保存后同步所有窗口
│  │     │  ├─ 跟随系统 [A103] — 监听窗口系统外观并自动解析浅色/深色
│  │     │  ├─ 晨雾浅色 [A104] — 默认浅色主题
│  │     │  └─ 深海夜色 [A105] — 低眩光深色主题
│  │     ├─ 显示行号 [A095] — 标准 Switch；控制新文件的行号列默认值
│  │     ├─ 行号行间分隔线 [A308] — 标准 Switch；隐藏行号时禁用
│  │     ├─ 行号栏宽度 [A299] — 标准 Slider；40–160px、步长 1
│  │     ├─ 行号文字颜色 [A300/A301] — 标准 ColorPicker + 恢复主题色 Button
│  │     ├─ 行号背景色 [A302/A303] — 标准 ColorPicker + 恢复主题色 Button
│  │     ├─ 日志分隔线 [A096] — 标准 Switch；控制新文件的弱分隔线默认值
│  │     ├─ 日志级别着色 [A177] — 标准 Switch；控制语义色整行强调
│  │     ├─ 日志字体 [A163] — 标准 Select；支持四种等宽字体来源
│  │     ├─ 日志字号 [A161] — 标准 Slider，8–32px、步长 1
│  │     ├─ 日志行距 [A162] — 标准 Slider，默认 6px、范围 1–40px、步长 1
│  │     ├─ 搜索结果 [A249] — “搜索”页应用级搜索输出策略
│  │     │  ├─ 区分大小写 [A253] — 标准 Switch
│  │     │  ├─ 使用正则表达式 [A254] — 标准 Switch
│  │     │  ├─ 最大搜索结果数 [A250] — retained 标准 NumberInput；0 表示不限制
│  │     │  └─ 高亮匹配文字 [A251] — 标准 Switch
│  │     ├─ 搜索历史管理 [A382] — “搜索”页不限数量的历史草稿；保存后写入 SQLite 并同步所有窗口
│  │     │  ├─ 历史筛选 [A383] — retained 标准 Input；按查询文字过滤管理列表
│  │     │  ├─ 历史计数 [A384] — 显示筛选结果数与历史总数
│  │     │  ├─ 清空全部 [A385] — 仅修改对话框草稿，保存后生效
│  │     │  └─ 历史列表 [A386] — 固定高度虚拟列表与唯一纵向滚动所有者
│  │     │     └─ 历史管理项 [A387] — 查询原文作为稳定身份，单行省略显示
│  │     │        └─ 删除历史项 [A388] — 仅修改对话框草稿，保存后生效
│  │     ├─ 文件与标签 [A246] — “常规”页应用级路径呈现与关闭安全选项
│  │     │  ├─ 显示完整路径 [A247] — 标准 Switch
│  │     │  ├─ 关闭标签前确认 [A248] — 标准 Switch
│  │     │  └─ 打开目录命令 [A298] — retained 标准 Input；支持 `{directory}` / `{path}`
│  │     ├─ 滚动与动态效果 [A240] — “滚动与交互”页应用级滚轮策略
│  │     │  ├─ 按完整日志行滚动 [A241] — 标准 Switch
│  │     │  ├─ 每次滚动行数 [A242，按行模式] — 标准 Slider，1–100 行
│  │     │  ├─ 自动换行仍按行滚动 [A243，按行模式] — 标准 Switch
│  │     │  ├─ 像素滚动距离 [A244] — 标准 Slider，1%–400%
│  │     │  ├─ 相邻行预读取 [旧版 A160] — 标准 Slider，4–40 行
│  │     │  ├─ 分词边界字符 [A252] — retained 标准 Input；最多 256 个 Unicode 字符
│  │     │  └─ 减少动态效果 [A245] — 标准 Switch
│  │     ├─ 索引缓存设置组 [A189] — “存储”页缓存占用与操作
│  │     │  ├─ 缓存大小 [A190] — 后台统计受管理索引和暂存文件
│  │     │  ├─ 打开缓存文件夹 [A191] — 交给系统文件管理器揭示
│  │     │  ├─ 清理缓存 [A192] — 仅删除复核后未变化的受管理文件
│  │     │  └─ 缓存操作状态 [A193，条件呈现] — 就地显示操作结果
│  │     ├─ 应用日志 [A477] — “高级”页诊断设置；只保留最近 20000 条且总量不超过 8 MiB
│  │     │  ├─ 日志等级 [A478] — 标准 Select；关闭或选择 Error/Warn/Info/Debug/Trace 最低等级
│  │     │  ├─ 导出日志 [A479] — 标准 Button；通过系统路径选择器导出当前缓冲快照
│  │     │  └─ 日志状态 [A480，条件呈现] — 当前缓冲数量及最近一次导出结果
│  │     ├─ 快捷键设置 [A155] — “快捷键”页录入草稿与已生效绑定隔离
│  │     │  └─ 快捷键列表 [A156] — 固定高度的唯一纵向滚动所有者
│  │     │     └─ 快捷键操作项 [A157，可重复] — 操作名称构成稳定身份
│  │     │        ├─ 快捷键录入框 [A158，可重复] — 只读标准 Input
│  │     │        ├─ 恢复默认快捷键 [A159，可重复] — 标准小型幽灵 Button
│  │     │        └─ 快捷键冲突 [条件呈现] — 主题危险色就地提示并阻止保存
│  │     └─ 关于信息 [A396] — “关于”页版本与项目信息；不参与设置草稿与保存
│  │        ├─ 应用摘要 [A397] — VCLogg2 名称、用途与带“2.0”徽章的嵌入式项目 ICO 图标
│  │        ├─ 版本与构建 [A398] — 包版本、编译 Commit、本地时区编译时间、目标三元组与 Debug/Release 配置
│  │        ├─ 技术栈 [A399] — Rust、GPUI、数据检索、网络通信与原生平台集成概览
│  │        ├─ 主要开源库 [A400] — 核心直接依赖及其在应用中的职责
│  │        ├─ GitHub 仓库 [A401] — 显示仓库地址；标准语义 Link，点击或键盘激活后由系统浏览器打开
│  │        └─ 作者与许可 [A402] — 作者名称、Apache 2.0、Copyright 与第三方权利声明
│  ├─ 颜色标签对话框 [A167] — 与 A085/A094/A198 共用管理类 Dialog 的风格、尺寸和窗口居中规则；编辑草稿，保存后一次提交并跨窗口刷新
│  │  ├─ 标签说明 [A168] — 说明列表顺序控制 Ctrl+D 轮换，颜色修改影响引用该 ID 的文件规则
│  │  ├─ 标签列表 [A169] — 占满对话框剩余高度的唯一纵向滚动所有者；允许保存空集合
│  │  │  └─ 标签编辑项 [A170，可重复] — 持久标签 ID 构成稳定身份
│  │  │     ├─ 颜色预览 [A171] — 直接显示当前数据色，不承担唯一状态说明
│  │  │     ├─ 标签名称 [A172] — 标准 Input；空名称阻止保存并保留对话框
│  │  │     ├─ 标签颜色 [A173] — 标准 ColorPicker；提供调色板、HSLA 与十六进制输入
│  │  │     └─ 删除标签 [A174] — 只修改草稿，取消对话框可恢复
│  │  └─ 添加标签 [A175] — 标准 Button；创建持久自定义 ID 并聚焦新名称
│  ├─ 预定义过滤器对话框 [A198] — 与 A085/A094/A167 共用管理类 Dialog 的风格、尺寸和窗口居中规则；始终保留列表页面，外层禁止滚动
│  │  ├─ 标题区 [A442] — 直接使用 gpui-component 原生 DialogTitle 与标准关闭按钮，不再重复绘制高标题带
│  │  ├─ 本地 / 云端页签 [A211] — 标准下划线 TabBar；保留同一对话框实体，已有会话首次进入云端页会自动加载第一页
│  │  ├─ 本地过滤器面板 [A443] — 固定工具栏与表头，表体占满剩余高度
│  │  │  ├─ 本地工具栏 [A444] — 左侧新增、分享与删除，右侧导入与导出
│  │  │  │  ├─ 添加过滤器 [A199] — 生成唯一且不可变的 UUID，选中新分支、打开 A456 并聚焦名称
│  │  │  │  ├─ 分享 [A445] — 紧凑小号按钮；仅在服务器声明 UUID 分支协议时打开 A220，并只预选当前勾选且从未发布到当前服务器的本地项
│  │  │  │  ├─ 删除 [A470] — 紧凑小号按钮；当前勾选为空时禁用，危险确认后只删除本地副本，不删除已经发布的云端过滤器
│  │  │  │  ├─ 导入过滤器 [A200] — 系统单文件选择；后台读取 VCLogg2 v1–v5/数组 JSON 或旧版 Qt QSettings CONF/INI，按 UUID 追加、折叠或三方合并，不删除文件中未出现的本地项
│  │  │  │  └─ 导出过滤器 [A201] — 系统另存为；写出 `{ version: 5, filters: [...] }` JSON
│  │  │  └─ 本地过滤器表格 [A202] — 列为多选、名称、匹配值、类型、操作；允许保存空集合及不同 UUID 的同内容项，名称列不拼接 UUID
│  │  │     ├─ 全选本地过滤器 [A468] — 选择或清空当前全部本地过滤器
│  │  │     ├─ 本地表体滚动视口 [A446] — 在受约束的剩余高度内作为唯一纵向滚动所有者；鼠标滚轮和 A447 拖动共用 retained ScrollHandle
│  │  │     │  └─ 本地过滤器行 [A203，可重复] — UUID 只构成稳定控件身份，不显示在名称中；详情当前项以左侧主色条强调
│  │  │     │     ├─ 选择本地过滤器 [A469，可重复] — 修改批量操作选择，不打开详情
│  │  │     │     ├─ 行摘要按钮 [A455] — 名称、匹配值与类型组成单一可聚焦入口；点击或键盘激活时打开 A456，鼠标点击不保留焦点边框，键盘导航仍显示可见焦点
│  │  │     │     │  └─ 类型徽标 [A448] — 以主题色区分“正则”和“文本”，不把截图样例硬编码为固定类型
│  │  │     │     ├─ 首次分享 [A449] — 只选择尚未跟踪当前服务器的分支并打开 A220；未连接、协议不支持或草稿无效时禁用，已有远程引用的变化改从 A462 显式提交
│  │  │     │     └─ 删除过滤器 [A209] — 打开危险 AlertDialog，确认后只删除草稿项
│  │  │     └─ 本地表格滚动条 [A447] — 常驻表格右边界，不进入固定表头和工具栏
│  │  ├─ 过滤器分享弹窗 [A220，二级标准 Dialog] — 复用详情弹窗的尺寸、观察 surface、原生 DialogTitle、标准关闭与焦点恢复；只列出未发布到当前服务器的本地 UUID 分支，明确勾选后首次发布
│  │  ├─ 本地过滤器详情弹窗 [A456，二级标准 Dialog] — 覆盖于 A198 上方而不替换主列表；关闭或 `Esc` 只关闭顶层详情并恢复原行焦点
│  │  │  ├─ 本地详情标题 / 页签 [A457] — 原生 DialogTitle 显示“本地过滤器详情”并提供标准关闭按钮，内容区保留详情页签
│  │  │  ├─ 本地详情表单 [A450] — 独立滚动内容区，不再插入表格行内
│  │  │  │  ├─ 名称 [A205] — 标准 retained Input；空白在保存或确定时阻止提交
│  │  │  │  ├─ 匹配值 [A207] — 标准 retained Input；空白阻止提交，文本可包含正则本身的 `|`
│  │  │  │  ├─ 备注 [A206] — 标准 retained Input；可空并随云端元数据保留
│  │  │  │  └─ 正则 / 允许共创 [A208] — gpui-component Switch；共创值为后续云端分享保留
│  │  │  ├─ 完整 UUID [A210] — 只读显示并提供复制；名称或内容相同的分支仍可据此区分
│  │  │  └─ 本地详情操作栏 [A458] — 上移、下移和完成；完成后返回原行
│  │  ├─ 云端过滤器面板 [A212] — 连接与目录浏览各自持有明确加载/成功/失败状态
│  │  │  ├─ 云端连接面板 [A213，未连接时] — 标准 Input 填写服务器和显示名称；成功后公开配置写入 SQLite，会话秘密只进系统凭据库
│  │  │  ├─ 云端工具栏 [A451，已连接时] — 身份菜单、搜索框、单一排序菜单、搜索与批量下载保持固定
│  │  │  │  ├─ 连接 / 重新连接 / 断开 [A214] — 首次连接直接注册客户端身份；身份菜单可重连或断开，断开只清运行时会话
│  │  │  │  ├─ 搜索与排序 [A215] — 名称、关键词、备注或分享者查询；最新、下载最多、点赞最多三种服务端排序
│  │  │  │  └─ 下载 / 更新所选 [A218] — 新 UUID 以相同 UUID checkout 到本地；先应用快进与无冲突合并并立即持久保存，再按所选顺序进入 A467 处理冲突
│  │  │  ├─ 云端过滤器表格 [A216] — 列为多选、名称、匹配值、类型、操作；表头支持选择当前页，名称列不拼接 UUID
│  │  │  │  ├─ 云端表体滚动视口 [A452] — 在受约束的剩余高度内作为唯一目录滚动所有者；鼠标滚轮和 A453 拖动共用 retained ScrollHandle，分页替换完整页并清空跨页选择
│  │  │  │  │  └─ 云端过滤器行 [A217，可重复] — UUID 只构成稳定控件身份，不显示在名称中；名称、匹配值和类型组成单一详情按钮；操作区显示点赞数、下载数与未下载/已同步/本地有修改/远程有更新/可自动合并/存在冲突/远端已删除/协议不支持状态
│  │  │  │  └─ 云端表格滚动条 [A453] — 常驻表格右边界，不进入固定表头和工具栏
│  │  │  ├─ 云端分页 [A454] — 表格下方居中显示上一页、当前页与总数、下一页
│  │  │  └─ 离线目录状态 [A238，条件呈现] — 显示缓存年龄与只读边界；保留搜索、排序、翻页、详情和本地导入，禁用依赖服务器的写操作
│  │  ├─ 云端过滤器详情弹窗 [A459，二级标准 Dialog] — 点击目录行后在 A198 上方即时弹出只读目录快照，在线后台刷新最新详情；重复激活不会叠加相同弹窗，关闭或 `Esc` 只关闭顶层详情并恢复原行焦点
│  │  │  ├─ 云端详情标题 / 页签 [A460] — 原生 DialogTitle 显示“云端过滤器详情”并提供标准关闭按钮，内容区保留详情 / 编辑记录页签
│  │  │  ├─ 云端详情内容 [A461，详情页签] — 完整 UUID、名称、匹配值、备注、正则、共创以及分享者、修订、点赞 / 下载、更新时间；UUID 可复制，全部业务字段只读
│  │  │  │  ├─ 下载 / 更新 / 合并 [A221，按状态] — 未下载时创建同 UUID 本地分支；远程有更新时快进；双方不重叠时安全合并；字段冲突进入 A467
│  │  │  │  ├─ 提交本地修改 [A219，按权限] — 已有同分支引用且本地有修改时携带共同基线修订提交；不会由下载或合并自动触发
│  │  │  │  ├─ 修订冲突提示 [A222，条件呈现] — 结构化 409 后读取 `currentRevision` 与远端最新版，安全合并或进入 A467，绝不自动重试覆盖
│  │  │  │  └─ 删除云端分享 [A224] — 仅所有者可见的危险确认；成功后保留本地内容并解除对应云端引用
│  │  │  ├─ 修订历史 / 快照 [A223，编辑记录页签] — 每页 30 条的服务端分页；显示编辑者和完整只读内容，“恢复到本地”形成待提交的本地修改
│  │  │  └─ 云端详情操作栏 [A462] — 在详情与编辑记录页签始终固定显示；下载/更新/合并结果立即持久保存，另有提交本地修改、另存为新过滤器、删除分享及右下角关闭；另存生成新 UUID 并保留派生来源
│  │  ├─ 过滤器冲突页面 [A467，二级标准 Dialog] — 与详情和分享互斥地占用同一个二级路由；逐字段并列本地与远程值，可单项选择、全部保留本地、全部采用远程或稍后处理；完成后更新本地内容与基线但不自动提交
│  │  └─ 固定底栏 [A370] — 使用标准 DialogFooter 靠右下排列“取消”“保存”“确定”；确定校验、保存并关闭，取消丢弃尚未保存的草稿，保存提交但保持窗口打开；云端页只显示右下角“关闭”
│  ├─ 全局搜索文件对话框 [A105] — 标准 Dialog；草稿与已提交参与集合隔离，保存后一次应用
│  │  ├─ 参与文件计数 [A106] — 已选择数量与当前打开总数
│  │  ├─ 全选文件 [A107] — 选择全部当前打开标签
│  │  ├─ 清空文件 [A108] — 允许保存空集合；主搜索按钮随后禁用
│  │  ├─ 参与文件列表 [A109] — 固定高度的唯一纵向滚动所有者
│  │     └─ 参与文件项 [A110，可重复] — gpui-component Checkbox；文档 ID 构成稳定身份，显示文件名和完整路径
│  │  └─ 保存 / 取消 [A433] — 固定在对话框底部；保存一次提交参与集合，取消不改变已提交集合
│  ├─ 目录搜索设置对话框 [A428] — 标准 Dialog；目录、文件类型过滤和递归选项由 Workspace 持久化，重开应用或再次打开对话框时恢复；取消不修改已保存配置
│  │  ├─ 选择搜索目录 [A429] — 只允许选择一个本地目录；显示完整路径并就地校验空选择；保存后持久化上次目录
│  │  ├─ 文件类型 [A430] — Checkbox；勾选时按 A466 中的后缀过滤，取消勾选时搜索所有文件；勾选状态与输入内容一并持久化
│  │  │  └─ 文件后缀 [A466] — gpui-component Input；多个大小写不敏感的文件后缀使用 `|` 分隔，例如 `*.txt|*.log`，A430 未勾选时禁用但保留输入
│  │  ├─ 包含子目录 [A431] — Checkbox，默认勾选
│  │  ├─ 包含隐藏目录 [A432] — Checkbox；仅在包含子目录时可用，默认不勾选
│  │  └─ 保存 / 取消 [A434] — 固定在对话框底部；保存一次提交目录配置，取消不改变已保存配置
│  ├─ 重命名标签对话框 [A133] — 标准 Dialog；说明只改变显示名称，不重命名磁盘文件
│  │  ├─ 标签名称输入框 [A134] — gpui-component Input；打开后聚焦并全选当前显示名称
│  │  └─ 空名称校验 [A135，提交空白名称后] — 主题危险色就地说明并保持对话框与输入焦点
│  └─ 文件历史对话框 [A085] — 与 A094/A167/A198 共用管理类 Dialog 的风格、尺寸和窗口居中规则；标题、页签、筛选框与统计操作区固定且不可收缩，外层内容禁止滚动；按需加载且关闭后不保留后台任务
│     ├─ 历史筛选 [A086] — gpui-component Input；按文件名、完整路径或保存查询即时筛选
│     ├─ 文件记录 / 临时搜索结果 [A228] — 两个标准 Button 页签；计数由本次打开时的数据库查询与系统临时目录扫描派生
│     ├─ 历史计数/安全说明 [A087] — 当前筛选数量、文件 Session 总数、SQLite 主库/WAL/SHM 合计占用及“不会删除日志文件”说明
│     ├─ 清除历史 [A082] — 仅在文件记录页显示；关闭文件历史对话框后打开危险操作确认
│     ├─ 历史记录列表 [A088] — 占满固定控制区下方的全部剩余高度；主题边框与背景同时包裹滚动视口和右侧滚动条
│     │  ├─ 历史记录滚动视口 [A438] — 文件记录页唯一纵向滚动所有者；支持鼠标滚轮，并与 A439 共用 retained ScrollHandle
│     │  │  └─ 历史记录 [A089，可重复] — 数据库会话 ID 构成稳定身份；与最近文件一致的单行稳定列依次显示文件图标、文件名、目录、本地时间和右侧操作，恢复行、查询、标记与 revision 收进“打开”提示，保护原因保留在删除提示
│     │  │     ├─ 打开历史文件 [A090，可重复] — Workspace 接收打开事件后关闭对话框，并沿用普通会话恢复路径，避免发送者提前释放导致事件丢失
│     │  │     ├─ 删除恢复记录 [A091，可重复] — 受保护时禁用；只进入行内确认，不立即删除
│     │  │     └─ 删除行内确认 [A092，条件呈现] — 确认/取消；后台事务再次检查打开、收藏与标记保护
│     │  └─ 历史记录滚动条 [A439] — 内容溢出时常驻在列表右边界；支持点击轨道与拖动滑块，不进入上方固定控制区
│     ├─ 清理全部临时结果 [A232] — 临时结果页固定操作；批量危险确认，只处理未打开项并逐文件移入系统回收站
│     └─ 临时搜索结果列表 [A229] — 占满固定控制区下方的全部剩余高度；只枚举系统临时目录直属的 `vclogg2-search-*` 文件
│        ├─ 临时结果滚动视口 [A440] — 临时结果页唯一纵向滚动所有者；支持鼠标滚轮，并与 A441 共用独立 retained ScrollHandle
│        │  ├─ 临时结果项 [A230，可重复] — 显示文件名、完整路径、大小和跨窗口打开保护状态
│        │  └─ 删除临时结果 [A231，可重复] — 打开危险 AlertDialog；当前任一窗口打开时禁用
│        └─ 临时结果滚动条 [A441] — 内容溢出时常驻在列表右边界；支持点击轨道与拖动滑块，不进入上方固定控制区
├─ SheetLayer [A043] — 补充工作面板层
└─ NotificationLayer [A044] — 文件打开与搜索错误反馈层
```

控件编号是稳定身份，不与渲染顺序或集合下标绑定。

## 键盘路径

- `Ctrl+O`（可在设置中修改）→ 同步注册不执行文件 I/O 的稳定“打开中”标签 → 最多 1 MiB / 200 行的有界安全预览 → 在同一稳定标签、表格与虚拟列表实体中原子换入完整索引。若严格强身份校验通过且会话保存了源行，预览会直接读取该行前后约 200 行并保持全局行号；否则安全回退文件头。预览阶段保留正文浏览、选择与复制，搜索、页内查找、按行定位、文件末尾和末尾跟随会等到完整索引就绪；打开中或预览标签被关闭后，迟到的预览与完整结果都按路径和文档 ID 丢弃，不会重新打开标签。完整索引失败时保留安全预览并允许重新加载。会话中位于预览范围外的选择与标记作为待恢复领域行保留，完整文档到达后再合并；预览内的新用户选择优先于旧锚点。
- 进程命令行中位于可执行文件之后的每个路径按原顺序转换为初始文档，并在首个 Workspace 的状态库 bootstrap 后进入与 `Ctrl+O` 相同的空壳、预览与完整索引链路；命令行路径不构造默认覆盖，因此会恢复已有会话。文件关联或终端启动不维护第二套加载状态。
- 预定义过滤器弹层只把匹配值作为完整 `|` 分隔段加入或移除，不会因普通子串相同误判选中；正则匹配值中的 `|` 仍作为过滤器内容整体参与边界判断。搜索执行层可精确避免重复查询段，但实体列表只按 UUID 识别身份：名称、内容、类型、备注与共创状态全部相同的不同 UUID 仍完整展示、保存与同步。Checkbox 动作立即更新当前范围的查询草稿并保持弹层开启，正则项只在加入时自动开启正则，不会在移除后擅自关闭用户的正则模式；`Esc` 或外部点击关闭弹层并把焦点还给触发器。过滤器管理的第一层与第二层弹窗统一使用原生 DialogTitle；本地详情、云端只读详情、首次分享和 A467 冲突处理共用单一二级路由与 Dialog surface，任何时刻只有一个二级页面。`Esc` 只关闭最上层弹窗并由 Root 恢复原触发焦点，不关闭外层对话框。保存编辑器后 SQLite 顺序与所有窗口的弹层一次同步，取消外层对话框不会改变已提交集合。
- 云端过滤器页在已有凭据会话时首次进入会自动加载目录；搜索、排序和翻页各自产生新的一页快照。每个在线分页会按标准化服务器地址隔离写入公开目录缓存，不保存 Cookie、CSRF 或身份凭据；服务不可达时 A238 汇总此前已浏览分页并在本机完成搜索、排序和分页。离线目录只允许把严格晚于共同基线的缓存修订下载或安全合入本地，不得回退基线；点赞、首次分享、提交、删除和修订读取/恢复保持禁用。批量下载先应用新分支、快进与无冲突三方合并，再顺序呈现字段冲突；“稍后处理”保持对应本地分支不变。合并后的本地内容不会自动提交。在线提交使用跟踪引用中的 `baseRevision`；结构化修订冲突只触发刷新与三方合并，不通过错误文本判断，也不自动重试。服务器未声明 `filter-uuid-branches-v1` 时目录浏览、缓存和下载仍可用，首次分享与远程提交明确禁用并提示升级。
- 主窗口不执行启动或后台版本检测，也不下载、暂存或安装发行包。A355 在所有构建配置下直接通过系统浏览器打开 `https://github.com/zhaiyanqi/vclogg2/releases`；A403 使用 `关于 ver.<当前版本>` / `About ver.<current version>` 文案，并继续打开设置对话框的“关于”分类。
- 文件历史打开时同时扫描系统临时目录下的 VCLogg2 搜索结果。普通文件记录与临时结果共用筛选输入但各有一个固定高度滚动列表；临时结果不进入 SQLite Session、最近文件或恢复集合。删除前从所有已注册 Workspace 汇总打开路径，任何窗口仍持有的临时结果都会禁用；确认后逐个调用当前平台系统废纸篓，成功项再尝试移除已空的直属临时目录。
- 平台安装器在当前用户范围注册文件打开能力：Windows 写入应用能力与 `OpenWithProgids` 但不写 `UserChoice`，macOS bundle 声明文档类型，Linux 写入 `.desktop` 与 MIME 列表。终端和系统文件打开事件传入的一个或多个路径继续复用单实例与普通打开链路。
- “视图”菜单在打开时按当前窗口状态提供“进入全屏 / Enter full screen”或“退出全屏 / Exit full screen”；macOS 可用标准 `⌃⌘F` 或兼容的 `F11`，Windows/Linux 使用 `F11`，均只切换当前窗口的平台全屏状态。macOS 窗口模式为原生三色窗口按钮保留约 80px 外层起始间距；全屏后系统隐藏三色按钮，TitleBar 外层占位归零，组件内部仍保留约 12px 紧凑边距。
- `Ctrl+Shift+N` 或“新窗口”创建新的原生窗口并立即激活；每个窗口独立拥有标签、活动文件、搜索、历史弹层和通知状态。正常关闭单个窗口会把它移出恢复集合；整应用退出时按窗口创建顺序收集仍存活窗口，路径去重后用一次事务保存文件会话、标签并集和首个有效活动标签，下一次启动的“上一次文件”可恢复跨窗口集合。
- 标签菜单“复制到新窗口”在点击提交点捕获路径、自定义标题、源行位置、查询选项、结果模式、标记和呈现设置。新窗口完成状态库 bootstrap 后用该快照重新索引并一次恢复标签；源窗口标签、选择和搜索不变，临时结果标签继续保持不进入最近文件或恢复清单的边界。
- “移动到新窗口”复用同一不可变传输快照，但只在目标文件成功建立索引且源标签当前会话仍与点击时完全一致时关闭源标签。目标失败时明确保留源标签；传输期间源行、查询、标记、显示选项或名称发生变化时，目标保留已打开副本并提示源标签因变化而保留。重复移动同一标签会被阻止。
- “移动/复制到另一窗口”由应用级窗口注册表选择最近获得焦点的其他原生窗口。目标通过自己的 Workspace 接收并重新索引传输快照；目标正在打开文件、已经打开同一路径或已关闭时会给出明确反馈。移动仍遵守 A142 的成功确认与源快照一致性检查，立即拒绝通过延迟回执完成，避免窗口间嵌套更新。
- 从文件资源管理器向窗口任意工作区位置拖入一个或多个文件时，会显示覆盖整个工作区的“松开以打开日志”反馈；离开窗口后自动消退，松开时立即关闭。文件保持平台给出的顺序并进入与 `Ctrl+O` 相同的后台索引链路；文件夹被忽略，若当前正在打开其他文件则明确提示稍后重试。
- `F5` → 后台建立新快照与当前搜索 → 原子替换活动文档。
- 编码菜单显示本轮文档快照的自动检测结果；“重新检测并加载”复用 `F5` 的保留旧视图、后台构建和原子换入路径，“复制编码名称”只复制当前名称。检测顺序为 BOM、已知二进制签名、无 BOM UTF-16 启发式、控制字节二进制判定、UTF-8 与传统编码检测；Binary 固定按每行 16 字节显示小写十六进制，搜索与导出使用相同可见文本。
- `Ctrl+F`（可修改）→ 搜索输入框并全选 → `Enter` → 后台搜索 → 结果面板。
- `Ctrl+Shift+F`（可修改）按用户最后一次实际点击的正文、当前结果或扁平全局结果锁定区域，并在该区域右上角打开不占布局的不透明页内查找浮层；结果行触发的正文联动定位和程序化焦点变化不改写该区域，因此点击结果后仍会在结果区打开。默认采用大小写不敏感的完整字面量，`|` 与正则字符均按普通文本处理；A463–A465 可独立组合大小写、全词与正则规则，切换后立即按同一匹配器重做定位，并把各标签正文、当前文件结果与全局/目录结果的可见关键词同时叠加金色高亮，导航范围仍锁定打开时的区域。正则草稿无效或增量扫描没有命中时，浮层边框就地显示 danger 红色；已有命中后继续到达开头或末尾时仍显示 warning 黄色。输入框聚焦时，普通文字与编辑组合键只由 Input 消费，日志标记、自动换行、选择和其他应用快捷键均被上下文排除。输入变化 50ms 后协作取消旧扫描，从打开时的选择行向后增量定位。命中更新正文、当前结果或全局结果的表格选择与联动滚动时，表格监听器不接管输入框焦点，因此连续输入不会中断；用户已通过鼠标或键盘主动转移焦点时，表格仍按原规则获得焦点。`Enter` / `F3` 查找下一行，`Shift+Enter` / `Shift+F3` 查找上一行，均不跨边界循环；到达同一边界后重复命令不会重新扫描。命中行居中、成为当前区域选择并关闭末尾跟随。`Esc` 关闭并清除正文与结果中的页内高亮、把焦点还给锁定区域，但保留下次可复用的查询和匹配选项。
- 搜索成功后将完整非空查询按原文精确去重并置于历史首位，SQLite 不限制保存条数。鼠标悬停输入框滚动会先聚焦，首次滚动载入最新项，向下滚动查看更早查询、向上滚动查看更新查询且不会越过最新项。历史下拉菜单用固定行高虚拟列表呈现全部记录并保持最新优先顺序，点击只回填并全选，仍由用户按 `Enter` 提交。A382 可筛选、逐项删除或清空历史；管理动作只改设置草稿，保存时只删除打开设置时已存在且被移除的记录，以保留对话框开启期间其他窗口新增的搜索，并将结果同步到所有窗口。
- 输入当前顶层 `|` 片段后，A255 按大小写不敏感的包含关系组合历史分支和预定义过滤器候选。历史和正则过滤器只在未转义、字符组及括号之外的顶层 `|` 处分支；过滤器的分支候选排在完整多分支表达式之前并精确去重。列表打开时不预选；`↑`/`↓` 首尾循环并确保当前项可见，`Enter` 只在已有活动项时替换当前片段且不搜索，没有活动项时关闭列表并提交原文，`Esc` 只关闭列表。列表关闭时 `↓` 从最新完整历史开始、`↑` 从最旧完整历史开始；滚轮继续沿用不弹出列表的完整历史替换。当前片段为空时不自动展开匹配候选。
- 当前文件搜索把长度为零的输入视为空查询；全部已打开文件与目录搜索允许长度为零的输入提交，此时匹配集合为空，最终显示集合只由 A058/A111 是否包含标记决定。首尾空格、内部空格和仅由空格组成的内容在输入、历史、文件会话、字面量分段及正则编译间保持原样。字面量仍以 `|` 分隔多个非空模式，但不会裁剪各段边界空格。
- 单文件与全局搜索的命中行保存在压缩有序集合中；虚拟表只在绘制、选择、页内查找或导出当前所需位置时解析对应源行。结果数量、顺序、标记并集、折叠分组和选择恢复语义与普通有序行数组一致，但不会为大规模命中在各界面层重复展开完整行号列表。
- A250 为 0 时保留全部命中；非零时单文件在达到上限后的下一条实际命中处停止并标记为已截断，全局搜索对每个参与文件独立使用同一上限。当前结果摘要、全局文件组和全局总摘要都明确显示截断状态。设置变更不会隐式重跑已经完成或正在运行的搜索，但会立即更新所有窗口后续搜索、重载和会话恢复采用的上限。A251 保存后立即刷新正文、当前结果和全局结果的已提交搜索文字高亮，不改变结果集合，也不移除颜色规则、快速查找金色高亮或日志级别整行强调。
- A036/A253 与 A037/A254 分别是同一个应用级“区分大小写 / 正则表达式”状态。搜索栏、菜单或快捷键切换后立即同步所有窗口和所有标签并写入 SQLite；设置保存也立即应用到所有窗口。切换标签、切换当前/全局范围、清空查询或恢复文件会话只改变查询草稿与结果，不会重置这两个开关。正在执行或已经提交的结果仍保留执行时的匹配语义，迟到的搜索/重载回执不能覆盖用户后来切换的开关；启动状态库到达前发生的切换优先于数据库旧值并会在 bootstrap 后补写。
- A097 的 A471 左键打开 A422，不再循环切换范围；A472、A424/A426 的行尾配置按钮与选中范围后的右键入口分别复用 A105/A428。右键入口拦截默认焦点转移，因此打开和关闭配置页不会把 A097 误显示为整框聚焦。多标签轮次并发扫描提交时的参与标签；目录轮次先按 A430/A466/A431/A432 枚举候选，再以最多四个文件准备线程建立临时只读文档并复用同一匹配器。两种范围都汇总进度并在整轮结束后一次安装文件分组；取消或失败不会用部分结果替换上一份完整结果。
- 参与文件对话框中的全选、清空或逐项勾选只修改对话框草稿，保存后一次替换参与集合。新打开标签默认参与，关闭标签立即退出集合；空集合保留但禁用全局搜索。
- 目录搜索不会把候选文件批量创建成标签，只保留实际命中文件和同路径已打开文件的只读快照用于虚拟结果；后者让目录范围可把现有标记按 A111 合并或单独显示，零行文件组不会进入结果表。点击结果后沿普通打开链路创建或激活标签，并在完整索引换入后定位源行；不可读文件和子目录独立计数，不中断其他候选。目录枚举不跟随符号链接，隐藏目录同时识别点号名称与 Windows 隐藏属性。
- 全局结果模式切换与 `M` 修改标记只重组当前已提交分组，不启动扫描；选择和首个可见项按文件 ID/源行恢复，源行不再存在时回退到所属文件标题或相邻扁平索引。
- 全局文件标题有结果时只折叠或展开该组，无结果或失败时激活所属标签；选择全局命中会切换到所属标签并定位源行，同时保留全局查询草稿。
- 搜索期间进度状态带持续显示扫描范围和匹配数；“取消搜索”立即发出协作取消并保留上一份结果。
- 表格支持方向键、Home、End、Page Up 和 Page Down；`Shift+↑/↓`、`Shift+Page Up/Page Down`、`Shift+Home/End` 从当前锚点扩展压缩范围，文件分组标题不进入领域选择。结果行确认后在同一 Workspace 更新内完成结果选择、活动文档切换、正文源行选择、滚动恢复与末尾跟随关闭。正文、当前结果和全局结果各自持有可 Tab 到达的 FocusHandle；复制、全选、标记、颜色标签和页内查找按焦点归属执行，关闭菜单或页内查找后返回原区域。索引按 UTF-8 字节显示宽度记录最长行，制表符推进到下一个 8 列制表位且首行 BOM 不占宽度；所选等宽字体的实际 `ch` advance 决定横向画布。
- 工具栏“自动换行”或默认 `W` 按标签切换正文、当前结果和全局结果；该快捷键可在设置的第九项命令中修改、清除或恢复默认。关闭换行时三块区域各自在 DataTable 底部使用横跨所在日志区域完整宽度的常驻横向滚动条，右端不再为纵向滚动条保留底部空白槽；横向条与表格是同级布局，不覆盖日志或结果行，并按纵向滚动条宽度同步补偿建模内容宽度以保持完整拖动范围。两种模式共用同一个行高：字号加行距先按当前显示器缩放对齐到整数设备像素，再交给固定行高表格和换行列表，不换行的日志行在切换前后画出来一样高、行距也不会逐行忽大忽小。行号单元格在两种模式下复用同一套字体、行高、背景、右对齐、垂直居中与覆盖式分隔线绘制规则；换行模式只额外施加列宽和随逻辑行拉伸的布局约束，因此原本无需换行的日志切换模式时行号位置保持不变。换行行的高度由内容撑出，行分隔线因此画成行内绝对定位的 1px 覆盖层而不是 `border_b`，避免每行比虚拟列表槽位高出 1px。标记列与行号列在关闭换行时是 DataTable 的固定左列，横向滚动时保持可见，组件会在固定列组右边缘画一条 1px 竖线；换行模式按相同的固定列宽度在同一坐标补一条覆盖层竖线，两种模式的分栏线位置一致。该分栏线只属于日志结果行；全局搜索文件标题始终横跨整行，以与未换行模式相同的基础行高固定布局，并由同一个标题组件绘制背景与边界，不显示分栏线，也不随换行开关、活动行或表格悬停状态改变样式。垂直滚动条一侧两种模式都不画竖线。开启时取消横向画布，长日志按可用宽度形成可变行高；列表只解码并测量虚拟视口附近的行，测得高度写回 retained 尺寸向量并用最多 4096 项累计修正二分定位。拖动逻辑滚动条后，下一帧绘制前会先按目标逻辑位置同步测量整个目标视口，避免仍用基础行高布局造成文字叠压。搜索提交新结果集合时，当前文件按源行号、全局结果按文件与源行号迁移仍然存在且已经实测的行高，新进入首屏的结果在同一次提交中同步测量，因此不会先退回单行槽位再膨胀。切换前，三个区域都按真实视口几何选择锚点：当前选中行实际可见时使用它，否则使用首个实际可见行，同时记录该行顶边距离视口顶部的 Y。切换后通过统一的“定位逻辑行顶边到视口 Y”接口恢复同一稳定行；换行宽度确定和后续行高实测继续重放这一精确位置，不再推断切换时是否应滚到末尾。全局搜索文件分组展开或折叠时也以文件标题为稳定锚点，并在提交新扁平布局前按当前宽度同步测量首屏结果，首帧直接使用真实换行高度。只有内容边缘无法提供足够滚动空间时才夹取。切换前后三块区域的滚动状态互不覆盖；开关写入文件会话，默认关闭。
- `Ctrl+W`（可修改）关闭活动标签，焦点保持在工作区的下一个有效目标。
- 标签栏始终至少包含一个标签。启动没有初始文件、关闭最后一个标签或执行“关闭所有标签”后，会立即创建并激活一个名为“新标签页”的 A435；A436 可继续追加多个空白标签。空白标签只存在于当前窗口生命周期，不写入文件会话、工作区顺序或下次启动恢复数据。
- A435 与文件标签共享激活、关闭按钮、中键关闭、窗口内拖动排序、所有标签菜单以及关闭左侧/右侧/其他/全部命令；空白标签不会触发日志关闭确认，也不显示删除文件、路径、重载、重命名和跨窗口移动/复制命令。批量目标同时包含文件标签时，确认对话框只按文件标签计数，取消会保留整个目标集合。
- 通过外部文件拖放、打开对话框、收藏/最近/上次文件或其他普通文件打开入口开始一批打开时，如果 A435 当前激活，首个文件会占用该标签的原视觉位置，后续文件按批次顺序追加；若首个路径已在本窗口打开，则移除 A435 并激活已有文件标签。跨窗口标签传输仍服从明确的插入位置，不消费活动空白标签。
- 启用 A248 后，标签关闭按钮、`Ctrl+W` 和标签菜单的单个/批量关闭都先显示标准确认；确认后仍沿用同一会话保存与搜索取消链路，取消不改变标签。跨窗口移动成功后的源卸载不会重复询问。A247 只改变文件工具栏在完整路径和标签名称之间的呈现，不改变窗口标题、标签稳定身份或任何路径命令。
- 每个标签的省略号按钮和正文右键命中层打开同一个标准菜单；右键入口使用 gpui-component `ContextMenuExt` 的焦点恢复，只覆盖文字区域且不遮挡省略号与关闭按钮。关闭其他、左侧、右侧或全部会先抓取所有目标标签的最终会话，再一次移除文档和全局结果分组。若活动标签被批量关闭，优先激活原索引位置的相邻标签；菜单的路径命令始终作用于触发标签，即使它不是当前标签。
- 标签菜单不提供源日志删除、移动或磁盘重命名操作；关闭标签只卸载查看会话并保留普通会话恢复语义。系统回收站仅用于历史页中应用自有临时搜索结果的显式清理。
- “重命名标签…”打开短表单并全选当前名称；空白提交在输入框旁显示校验且不关闭。有效名称立即同步标签、窗口标题和已提交全局结果分组，并写入文件会话检查点；关闭、重启或从历史恢复后继续使用。菜单“恢复标签名称”清除自定义值并恢复磁盘文件名，不修改文件路径或磁盘内容。
- 拖动标签到另一标签前或轨道末端只重排同一个 `DocumentTab` 实体；活动文档按稳定 ID 恢复，不重新索引、不改变正文/结果选择。已提交全局结果组同步采用新标签顺序；非临时文件的完整顺序和活动项在一次 SQLite 工作区事务中保存，并在正常退出时再次最终写入。
- 标签拖出源窗口时，源 Workspace 把当前指针转换为物理屏幕坐标，并与应用注册表中的其他窗口及其最新标签边界比对。命中另一窗口的标签或末端区域会显示插入线，并在该位置安装完整快照；放到所有 VCLogg2 窗口之外则从 GPUI 显示器可见边界计算新窗口位置，窗口左上角靠近释放点且不会落到 Dock、任务栏或屏幕外。默认移动，松开前按住平台主修饰键则复制。移动仍等待目标索引成功并复核源快照；目标忙碌、重复、关闭或拖回源窗口时安全保留源标签，不会向系统暴露真实日志路径。
- 正文和当前文件结果表在鼠标按下时只激活一次当前行，DataTable 不再于松开时重复选行或滚动；自动换行时点击只露出一部分的行也保持原视口位置，不会为了显示完整行自动对齐。`Ctrl+点击` 切换离散行，`Shift+点击` 从锚点替换范围，`Ctrl+Shift+点击` 合并范围。无修饰键时，同一条逻辑日志内的拖动保持 UTF-8 安全的实际文字选择；固定行高表格与自动换行列表都按当前帧真实渲染行边界判断，只有指针真正进入第二条逻辑日志才隐藏文字选区并切换成整行范围，不使用估算行高推算目标。继续拖动可从原始行扩展或回缩，拖回起始行会恢复按下后已经提交的行状态以及原始字符到当前字符的文字选区，再次跨行可重新进入整行模式。`Ctrl`/`Shift` 手势从按下开始就抑制文字选择。正文、当前结果和全局结果的双击选词都使用 A252：每个配置字符与所有 Unicode 空白都是边界，默认包含常用中英文标点，留空时仅按空白分词；选区复制与颜色标签继续读取精确 UTF-8 字节范围。无修饰第三次及后续连续按下会清除文字选区并只提交所属整行，不把换行或下一行开头带入选区，也不启动行拖动。按住左键拖到当前视口上下 32px 边缘会按越界距离每 40ms 自动滚动 1–8 个逻辑行并继续扩展同一范围，固定行高与自动换行共用相同状态机；移回中部、松开、关闭标签或失去目标立即停止。正文、当前结果和全局结果分别保留选择与焦点，结果跳转完成后焦点返回触发区域，状态栏显示当前区域的选择数量。
- 表格聚焦时 `Ctrl+A` 会先清除实际文字选区，再以压缩区间选择当前区域全部行；全局结果的区间可以跨越文件组标题，但选择计数、绘制和命令只消费实际命中行。`Ctrl+C` 与工具栏“复制行”优先复制最终保留的文字选区，没有文字选区时再按区域顺序复制整行；若在跨行整行模式松手，隐藏的底层文字投影会先被清除，因此复制稳定落到整行范围。`Ctrl+Shift+C` 始终复制整行并包含各自源文件的源行号。全局结果的批量标记按文件分组原子更新；颜色标签只接受同一文件的选择，跨文件时明确拒绝。顶部“复制行 / 标记行 / 轮换颜色”与快捷键读取同一个当前区域归属，只有实际命中行计数为零时才禁用，不会在全局 `Ctrl+A` 后仍呈现不可用状态。普通页面的文字参与者使用 Root 默认选择作用域，正文、当前结果和全局结果仍通过独立焦点、真实指针命中和 `LogRegion` 隔离命令；选择参与者缓存最多保留 2048 个最近浏览行且不会随大文件无限增长。全局结果模式、标记或颜色规则刷新时，选择先压缩为“文件 ID + 源行集合”，重组后再映射到仍可见的结果行。
- `Ctrl+G` → 转到行对话框 → 输入有效源行号 → 选择并滚动正文行。
- `Ctrl+Home` / `Ctrl+End` 选择并定位到文件第一行 / 最后一行；末尾快捷键可修改，空文件给出通知且不创建无效选择。
- 开启“跟随末尾”会立即定位末行；活动文件大小或修改时间变化后，后台对已验证的纯增长扩展行索引，截断、替换或验证不确定时全量重建，再恢复查询并继续定位末行。主动滚动或选择到末行之前、点击任一结果行都会关闭跟随。
- “行号”和“分隔线”会同时刷新当前标签的正文与结果表，不改变源行选择、标记或结果顺序。
- A299–A303/A308 行号栏外观保存后立即同步所有窗口；正文、单文件结果、全局结果及换行/不换行两套视图共享同一列宽、主题回退色与独立行号分隔线。
- A102/A103–A105 以单一应用级主题偏好写入 SQLite：浅色、深色在保存后立即更新 GPUI 语义主题及所有已注册窗口；跟随系统额外监听原生窗口外观变化。新安装、旧数据库、已移除的主题值或其他未知值均回退晨雾浅色。
- A127/A298 在设置中保存最多 2048 个字符的自定义打开目录命令。空值把文件所在目录直接交给 GPUI 平台的系统打开链路；非空值按单引号/双引号拆成可执行文件与独立参数，替换 `{directory}` 和 `{path}`，两者都未使用时追加目录参数，并以文件目录作为工作目录直接启动，绝不经过 PowerShell、cmd 或其他命令解释器。未闭合引号、空命令或启动失败通过窗口通知反馈。
- A390 将云端公开配置集中到设置页：对话框“保存”会持久化服务器地址与用户名；“保存并测试”还会先写入 SQLite 并同步其他窗口，再由 retained 后台任务调用现有 `CloudClient` 完成地址规范化、无客户端密钥注册、Cookie 获取和 `/api/v1/me` 复用验证。成功后把服务器规范化结果与工作区连接状态一起更新；失败保留公开配置并在 A394 就地说明。Cookie 与 CSRF 始终只进入按服务器地址分帐的系统凭据库，设置实体、SQLite 和界面均不接触秘密；HTTP 地址仍允许用于受控局域网，但界面明确建议 HTTPS。
- `Ctrl+,`（可修改）或“设置…”打开设置。可校验的修改会立即预览到所有窗口；无效数字、无效或冲突快捷键等中间草稿不会污染运行状态。保存会一次提交有效草稿并写入 SQLite，取消、`Esc` 或关闭对话框会恢复打开前的设置与搜索历史；新文件默认行号、默认分隔线和默认搜索方式只在保存时提交，预览不会覆盖当前标签独立的视图与搜索状态。保存会一次校验九项现有操作的快捷键：重复的非空组合键在行内标红并阻止提交，清空表示解除绑定，单项可恢复默认；有效修改立即作用于应用键盘映射。A253/A254 保存后立即同步所有窗口的搜索栏、菜单、全部标签及全局搜索草稿。A478 实时调整进程级日志过滤：关闭时不打印也不收集；Debug 首次默认 Debug，Release 首次默认 Error，当前缓冲可通过 A479 导出。日志字体、字号与行距立即刷新全部窗口内的正文、当前搜索结果及全局结果，虚拟表格行高采用“字号 + 行距”且不丢失当前选择；日志区域内按住 `Ctrl` 滚动鼠标滚轮可按 1px 步长调整字号，边界为 8–32px，停止滚动 300ms 后保存。普通垂直滚轮在子表格消费事件前由区域捕获层统一处理：可按设备像素距离的 1%–400% 缩放，也可把任意非零输入替换为固定 1–100 条逻辑日志；按行模式以行顶前缀二分定位当前行，不通过浮点除法取整，因此从底部向上滚动后可立即反向向下滚动；自动换行时是否继续按完整日志行滚动可独立选择，横向/`Shift` 滚动与 `Ctrl`/`⌘` 字号手势不被改写。旧版 A160 在这里适配为 4–40 行、默认 12 的“相邻行预读取”：正文、当前结果和全局结果会在组件当前绘制范围上下同步预解码，减少滚动到新范围时的映射解码抖动；gpui-component 的虚拟器仍拥有精确节点范围，因此不会伪造行节点或破坏滚动锚点。分词边界保存前按 Unicode 字符数限制为 256，并立即更新所有窗口之后的双击选词。“减少动态效果”直接同步 GPUI 应用级偏好，gpui-component 动效据此降级。“日志级别着色”默认关闭，开启后按不区分大小写的独立单词识别 ERROR 类、WARN 类、INFO 类与 DEBUG/TRACE 类，错误优先级最高，并在正文、当前结果和全局结果用主题语义色弱背景一致呈现；不占用 A051 标记列。行选择背景只覆盖正文列并使用主题表格活动色，标记列与行号列在单选、多选时都保持原样：单行选择显示完整上下边界；相邻多选行隐藏内部边界与分隔线，合并为首行上边界、末行下边界的连续选区段；离散多选分别形成独立选区段。固定行高表格的组件当前行仅用于传递语义事件和滚动，提交后立即清除其整行覆盖层；正文列的领域选区是唯一可见选中态。手动标记始终由 A051 表达。设置同时保存新文件的默认行号与分隔线，并立即把当前标签切回这些默认值。工具栏随后单独切换只覆盖当前文件；已有会话恢复自己的保存值，bootstrap 到达不会覆盖用户已经修改的标签。
- 设置页的索引缓存组只统计系统缓存目录中 VCLogg2 的 `.vclog-index` 与 `{16 位十六进制}.tmp-*` 文件，不把 Session、日志或其他文件计入。清理提交前为每个文件保存大小与修改时间，删除前再次核验；正在写入、已变化、无法删除或无关文件均保留，完成后立即重读剩余大小。应用每次进程启动还会延迟执行一次同样带变化复核的自动维护：普通索引保留 90 天、暂存文件保留 24 小时，并按最旧优先把普通索引总量限制在 512 MiB。
- 空工作区先保持“打开日志文件”可操作，同时在后台读取最近文件；点击最近文件项会在后台建立索引、重跑已保存查询，再原子安装源行位置、结果模式、标记和视图选项。
- 标签切换、交互检查点和关闭会携带该标签最近确认的会话 revision；若另一窗口已先更新同一路径，只把本窗口相对基准实际改过的字段合并进最新记录，未在本窗口修改的查询、标记、名称和呈现选项保持另一窗口的新值。正常退出前会在一个事务中最终保存全部打开标签的会话、顺序和活动项；下次启动只在空工作区显示“上一次文件”，单项或“全部打开”均由用户显式触发索引，全部打开完成后回到上次活动标签。
- 自动末尾跟随检测到文件变大后，会先快速核对旧文件头尾，再逐个验证完整旧前缀的 4 MiB 摘要。全部一致才增量发布新增行；文件中段即使在头尾不变时被改写，也会安全回退为完整重新加载。验证和重建均在后台完成，旧快照在新快照提交前保持可见。
- “收藏文件”保存当前活动文件的固定状态；空工作区可按数据库会话 ID 重新打开收藏项，“清空收藏”只取消固定且会立即刷新最近文件排序。
- “清除历史…”位于文件历史对话框的文件记录页；确认后以一个事务删除未打开、未收藏且没有行标记的会话，保留日志文件并同步刷新收藏、上一次文件和最近文件快照。
- 文件工具栏“历史…”按需打开完整会话历史；筛选只改变可见条目。当前打开、已收藏或包含行标记的记录显示保护原因且不能删除；其余条目需行内二次确认，成功后同步刷新所有起始页文件集合，日志文件不改变。
- 日志表格或结果表格聚焦时，`M` 作用于当前区域的一行或多行：目标全部已标记时统一取消，否则补齐全部未标记行；输入框不响应此绑定。
- 工具栏“轮换颜色”或 `Ctrl+D`（可修改）优先读取实际文字选区并去除首尾空白；没有文字选区时，读取当前区域选中的完整非空行并精确去重。若目标文字均已有启用的大小写敏感规则则统一移除；否则按颜色标签列表顺序轮换并补齐规则。文件会话将规则写入 SQLite，重新打开后恢复；正文、当前结果和全局结果共用预编译字面匹配器。工具栏“颜色标签…”可新增、删除、重命名和取色；保存定义后引用同一标签 ID 的规则立即在所有窗口重新解析。
- 结果显示模式切换后，结果表立即按源行号刷新；“标记与匹配”会对同一行去重，结果选择仍定位正文源行。
- 结果面板“新标签打开”和“保存…”始终捕获点击时的完整已提交结果快照；当前文件按结果顺序写原始日志正文，全局范围按标签顺序写出非空分组，并在每组前写入完整文件路径标题。全局“按时间合并”识别行首 `MM-DD HH:mm:ss.1–6 位小数`（空格也可为 `T`）；没有时间戳的续行优先继承同文件此前最近时间，首批续行必要时向后探测首个时间，并按时间、标签顺序、源行号稳定归并，不写文件分组标题。写入期间只让对应导出命令进入 loading，不冻结搜索、滚动或标签操作。
- “保存…”先显示系统目标选择框；取消不会创建或覆盖文件。确认后在目标目录写入唯一暂存文件并同步落盘，成功才原子替换目标，失败清理暂存文件并保留已有目标。
- “新标签打开”先在唯一 `vclogg2-search-*` 临时目录完整生成结果，再沿用普通文件索引和标签安装路径；临时结果标签不进入最近文件、会话检查点或上一次工作区恢复。
